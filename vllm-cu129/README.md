# vLLM cu129

Run `./setup.sh` on Ubuntu to install uv, FFmpeg, and the locked vLLM/PyTorch
cu129 environment. This variant does not download or install the system CUDA
Toolkit or cuDNN; it requires a compatible NVIDIA driver.

The script opens the `vllm-cu129` virtual environment after setup.
